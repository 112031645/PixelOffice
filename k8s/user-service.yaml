# ============================================================
# K8s 部署 - 用户服务
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-user-service
  namespace: cloud
  labels:
    app: cloud-user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloud-user-service
  template:
    metadata:
      labels:
        app: cloud-user-service
    spec:
      containers:
        - name: cloud-user-service
          image: cloud-user-service:1.0.0
          ports:
            - containerPort: 8081
          env:
            - name: NACOS_ADDR
              value: "nacos.cloud:8848"
            - name: MYSQL_HOST
              value: "mysql.cloud"
            - name: REDIS_HOST
              value: "redis.cloud"
            - name: ROCKETMQ_NAMESRV
              value: "rocketmq-namesrv.cloud:9876"
            - name: MINIO_HOST
              value: "minio.cloud"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 1024Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cloud-user-service
  namespace: cloud
spec:
  selector:
    app: cloud-user-service
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
